// addInstallButton(groupId, featureId, installId, buttonText)
// groupId is the parameter group's id: SwarmUI derives it from the group name by keeping
// lowercase letters only, so "MiniMax H3 SPEED" becomes "minimaxhspeed" (the digit is dropped).
addInstallButton('minimaxhspeed', 'minimax_h3_speed', 'minimax_h3_speed', 'Install MiniMax H3 SPEED');
